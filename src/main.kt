import Saving
import java.lang.Exception

fun main()
{
    var testSaving:Saving =Saving("ház",50000.0,50000000.0)
    var mySavingManager = SavingManager()
   /* try {
        mySavingManager.addNewSaving(testSaving)
        mySavingManager.addNewSaving(testSaving)
    }
    catch (exp:Exception)
    {
        println(exp.message)
    }*/


    //println(testSaving.toString())
}